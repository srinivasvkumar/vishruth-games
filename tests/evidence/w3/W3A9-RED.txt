W3-A.9 RED EVIDENCE
===================
Date: 2026-09-17
Base: main @ eb3cc82 + A7 (843a66e) + A8 (41c7cfd) merged into wt/t_9d8237a9
Test file: tests/unit/score.test.ts
Command: npx vitest run --config config/vite.config.ts tests/unit/score.test.ts

RESULT: 9 failed | 41 passed (50 total)

FAILED TESTS (all in 'readStoredHighScore (W3-A.9 canonical helper)'):

1. Fix A: storage throws -> 0 (no crash)
   "returns 0 when storage.getItem throws (privacy mode)"
   → TypeError: readStoredHighScore is not a function
   (Catches Fix A — the canonical helper does not yet exist)

2. Fix A: storage throws -> 0 (no crash)
   "ScoreManager constructor does not throw on storage-unavailable"
   → Error: localStorage is not available
   at Object.getItem tests/unit/score.test.ts:409:17
   at ScoreManager.loadHighScore src/systems/Score.ts:142:30
   at new ScoreManager src/systems/Score.ts:79:27
   (Catches Fix A — loadHighScore has no try/catch, constructor throws)

3. Fix B: overflow cap -> MAX_SAFE_INTEGER
   "clamps stored value above MAX_SAFE_INTEGER to MAX_SAFE_INTEGER"
   → TypeError: readStoredHighScore is not a function
   (Catches Fix B — the canonical helper with clampScore does not yet exist)

4. regression: parseInt no-op floor
   "'5.7' -> 5 (parseInt truncates; clampScore floor is no-op)"
   → TypeError: readStoredHighScore is not a function

5. basic semantics: "absent -> 0"
   → TypeError: readStoredHighScore is not a function

6. basic semantics: "'500' -> 500"
   → TypeError: readStoredHighScore is not a function

7. basic semantics: "'abc' -> 0"
   → TypeError: readStoredHighScore is not a function

8. basic semantics: "'-5' -> 0"
   → TypeError: readStoredHighScore is not a function

9. basic semantics: "'0' -> 0"
   → TypeError: readStoredHighScore is not a function

ANALYSIS:
- Tests 1-3 catch the two bug fixes (Fix A: try/catch, Fix B: clampScore cap)
- Tests 4-9 are regression tests for basic semantics (all fail because the
  canonical helper does not yet exist — expected RED state)
- The 41 passing tests are all pre-existing tests (pure functions, ScoreManager,
  persistence) — no regressions in the base.

VERDICT: RED confirmed. 9 new tests failing as designed.
