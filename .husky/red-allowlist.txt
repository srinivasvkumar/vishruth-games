# ============================================================================
# D0.2 RED ALLOWLIST — pre-commit gate
# (boss ruling option (b), 2026-09-10 20:28 AEST; T1 commit pre-ruling
#  2026-09-10 20:35 AEST, approved with guardrails)
# ============================================================================
# Consumed by: scripts/red-gate-check.mjs (wired into .husky/pre-commit, in
# place of the plain `npx lint-staged`, for the duration of D0.2 only).
#
# Mechanism:
#   - The FULL vitest suite runs on every commit; NO test is skipped —
#     allowlisted files are still RUN; their failures are merely tolerated.
#   - `npm run lint` runs and is reported but is NOT a commit-gate during
#     D0.2 (pre-existing defect: ESLint 8.57.1 cannot load the flat-format
#     config/eslint.config.js via --config — flagged to orchestrator; see
#     tests/baseline/INVENTORY.md "Known infrastructure defects").
#   - The commit is blocked ONLY if a failing test file is not listed below.
#   - Fail-safe: if the suite output cannot be parsed reliably, the commit
#     is blocked (never allow on uncertainty).
#
# Entry format: one repo-relative test-file path per line. Text after '#'
# (same-line or whole-line) is a comment, ignored by the checker.
# No wildcards. No directory globs. No blanket skips.
#
# ----------------------------------------------------------------------------
# REMOVAL PROCEDURE (end of D0.2 / Week 1)
#   1. TEMP-D0.2-T1 entries: T3 (task 0.2.2 "Retroactive Tests for Existing
#      Code") aligns the RED assertions so every TEMP-listed file passes,
#      and T3's commit removes ALL TEMP entries from this file. If any TEMP
#      entry would survive T3's commit, T3 does NOT commit — it escalates to
#      boss with evidence (see T3 task comment).
#   2. PERMANENT entries: delete each when its Week 1 task turns it green
#      (see per-entry notes below); record each removal in the tracker.
#   3. When the entry list below is EMPTY:
#        a. restore .husky/pre-commit to the plain form:  npx lint-staged
#        b. delete this file (.husky/red-allowlist.txt)
#        c. delete scripts/red-gate-check.mjs
#      and record the restoration in the tracker.
#   4. T4 (D0.2 final verification) must assert this file lists EXACTLY the
#      2 PERMANENT files. Any deviation = failed gate, no sign-off.
#      Invariant: at D0.2 end the list has demonstrably shrank from
#      (2 permanent + 8 TEMP) to exactly 2, and retroactive-core.test.ts
#      passes the hook.
# ============================================================================

# PERMANENT entries (boss spec, option (b) — listed exactly):

# turns green with TDD-1.3 "Three.js rendering test" (Week 1, day 1):
# contains the intentional RED placeholder "should create a WebGL renderer
# (RED phase)" (expect(false).toBe(true)) + 4 skipped stubs.
tests/unit/hello-three.test.ts

# allowlisted per boss spec as a safety net for the mock-strategy suite;
# all 54 tests green at T1 (2026-09-10) — entry currently inert, retained
# because the ruling lists it explicitly; re-validated by TDD-1.1
# "Initialize project with testing infrastructure" (Week 1, day 1).
tests/unit/mock-strategy.test.ts

# ----------------------------------------------------------------------------
# TEMP entries — T1 commit ONLY (pre-ruling 2026-09-10 20:35 AEST,
# guardrail 1: derived EXACTLY from the T1 evidence full-suite run saved at
# tests/evidence/d02/T1-green.txt, 2026-09-10 20:48 AEST; explicit file
# list, no wildcards). ALL are removed by T3's commit. No new TEMP entries
# after T1 without an explicit per-commit boss ruling (guardrail 5).
# ----------------------------------------------------------------------------
# (empty as of D0.2 G5, 2026-09-11 — last two TEMP entries, constants.test.ts
# and player.test.ts, removed in G5's commit; entries before G5: game-loop +
# scene-manager removed in G4, retroactive-core in G3, obstacle +
# physics-system in G2, game in G1.)
