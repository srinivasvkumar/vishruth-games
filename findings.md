{
  "findings": "# Cluster Rush — Comprehensive Source Code Discovery Report\n\n## 1. Project Overview\n\n| Property | Value |\n|---|---|\n| **Game Name** | Cluster Rush / ClusterRush |\n| **Engine** | Godot 4.7 (Forward Plus render backend) |\n| **Entry Scene** | `res://scenes/main_menu.tscn` |\n| **Virtual Port** | 1280 × 720 (canvas init) |\n| **Stretch Mode** | `none` (aspect ratio ignored) |\n| **Export Target** | WebGL (single-threaded; no SharedArrayBuffer / COOP/COEP) |\n| **Compression** | Brotli |\n| **Total Levels** | 35 |\n| **MSAA 3D** | 2x |\n| **Mouse** | Hidden (`input_devices/pointing/show_mouse=false`) |\n\n---\n\n## 2. Scenes / Screens\n\n### 2.1 `scenes/main_menu.tscn` (Title Screen)\n- Root: `Control` → `MainMenu` with `main_menu_ui.gd`\n- **Node tree:**\n  - `CenterContainer/VBoxContainer/Label` — Title \"Cluster Rush\" (48px font)\n  - `CenterContainer/VBoxContainer/StartButton` — \"Start Game\"\n  - `CenterContainer/VBoxContainer/LevelSelectButton` — \"Level Select\"\n  - `CenterContainer/VBoxContainer/SettingsButton` — \"Settings\"\n  - `CenterContainer/VBoxContainer/CreditsButton` — \"Credits\"\n  - `SettingsPanel` (PanelContainer, initially hidden) — Overlay settings\n  - `PauseMenu` (Control, initially hidden) — Overlay pause menu\n- **Background:** Dark purple-blue (`Color(0.12, 0.12, 0.18)`)\n- **Button style:** Blue gradient with corner radius 6\n\n### 2.2 `scenes/level_select.tscn` (Level Select Screen)\n- Root: `Control` → `LevelSelect` with `level_select_ui.gd`\n- **Node tree:**\n  - `VBoxContainer/TitleLabel` — \"Select Level\"\n  - `VBoxContainer/LevelButtonsScroll` (ScrollContainer)\n    - `VBoxContainer/LevelButtonsScroll/LevelButtonsContainer` (GridContainer, 7 columns)\n  - `VBoxContainer/RestartButton` — \"Restart\"\n  - `VBoxContainer/BackButton` — \"Back to Menu\"\n- 35 dynamically generated level buttons, tier-colored, with star indicators\n\n### 2.3 `scenes/game.tscn` (Gameplay Scene)\n- **Root node hierarchy:**\n  - `Root` (Node) — `game_scene.gd` controller\n    - `World` (Node3D)\n      - `DirectionalLight3D` (45°, 30°, shadows enabled)\n      - `DirectionalLight3D2` (-30°, -20°)\n      - `ParticleEffects` (Node3D, `particle_effects.gd`)\n      - `Player` (CharacterBody3D, `player_movement.gd`)\n        - `RayCastLeft` (target: `(-1, 0, 0)`)\n        - `RayCastRight` (target: `(1, 0, 0)`)\n        - `GroundCheck` (target: `(0, -1, 0)`)\n        - `SFX` (AudioStreamPlayer3D)\n      - `Camera3D` (Camera3D, `first_person_camera.gd`, `current = true`)\n    - `GameUI` (Control, `layer = 1`, 2D overlay)\n      - `HUD` (Control, `hud.gd`)\n        - `LevelLabel` (center top, \"Level: 1 / 35\")\n        - `LivesLabel` (top-left, \"Lives: 3\")\n        - `TimeLabel` (top-right, \"Time: 00:00.00\")\n        - `ScoreLabel` (top-right below timer, \"Score: 0\")\n        - `ProgressBar` (bottom edge, horizontal progress bar)\n      - `LevelComplete` (Control, initially hidden)\n        - `Panel/VBox/Title` — \"Level Complete!\"\n        - `Panel/VBox/Bonus` — \"Time Bonus: 0 pts\"\n        - `Panel/VBox/NextBtn` — \"Next Level\"\n      - `GameOver` (Control, initially hidden)\n        - `Panel2/VBox2/Title2` — \"Game Over\"\n        - `Panel2/VBox2/FinalScore` — \"Final Score: 0\"\n        - `Panel2/VBox2/RetryBtn` — \"Retry\"\n        - `Panel2/VBox2/LevelSelectBtn` — \"Level Select\"\n        - `Panel2/VBox2/MainMenuBtn` — \"Main Menu\"\n      - `PauseMenu` (Control, initially hidden)\n        - `Panel3/VBox3/Title3` — \"Paused\"\n        - `Panel3/VBox3/ResumeBtn` — \"Resume\"\n        - `Panel3/VBox3/PauseLevelSelectBtn` — \"Level Select\"\n        - `Panel3/VBox3/PauseMainMenuBtn` — \"Main Menu\"\n      - `LoadingScreen` (Control, initially hidden)\n        - `LoadingPanel/LoadingVBox/LoadingLabel` — \"Loading...\"\n        - `LoadingPanel/LoadingVBox/LoadingBar` (ProgressBar)\n\n### 2.4 `scenes/end_screen.tscn` (Game Completion)\n- Root: `Control` → `EndScreen` with `end_screen_ui.gd`\n- `VBoxContainer/Label` — \"🏆 All Levels Complete! 🏆\" (36px)\n- `VBoxContainer/ScoreLabel` — \"Final Score: 0\" (24px)\n- `VBoxContainer/MainMenuButton` — \"Back to Main Menu\"\n\n### 2.5 `scenes/credits.tscn`\n- Root: `Control`\n- `VBoxContainer/Label` — \"Cluster Rush\" (48px)\n- `VBoxContainer/RichTextLabel` — \"A truck-climbing platformer.\\n\\nEngine: Godot 4.7\\n\\nBuilt with love.\"\n\n### 2.6 `scenes/test_simple.tscn`\n- Test scene with a simple \"TEST\" label\n\n---\n\n## 3. Autoloads (Singletons)\n\n| Script | Path | Purpose |\n|---|---|---|\n| **GameManager** | `autoloads/game_manager.gd` | Central game state, score, lives, level progression, save system |\n| **LevelManager** | `autoloads/level_manager.gd` | Level loading, dynamic level generation, collision layers, hazard management |\n| **AudioManager** | `autoloads/audio_manager.gd` | Audio volume control (placeholder SFX playback) |\n| **InputManager** | `autoloads/input_manager.gd` | Input buffering, coyote time, action mapping |\n| **WebGLBridge** | `scripts/utilities/webgl_bridge.gd` | *Commented out in project.godot* — JS bridge for Playwright E2E testing |\n\n---\n\n## 4. Input Map\n\n| Action | Key | Properties |\n|---|---|---|\n| `ui_left` | A (keycode 65) | deadzone 0.5, auto-repeat |\n| `ui_right` | D (keycode 68) | deadzone 0.5, auto-repeat |\n| `ui_up` | W (keycode 87) | deadzone 0.5, auto-repeat |\n| `ui_down` | S (keycode 83) | deadzone 0.5, auto-repeat |\n| `jump` | Space (keycode 32) | deadzone 0.5, no-repeat |\n| `climb` | Q (keycode 81) | deadzone 0.5, no-repeat |\n| `pause` | (empty keycode 4194310) | deadzone 0.5 — likely mapped via UI |\n| `reset` | R (keycode 82) | deadzone 0.5 |\n| `strafe_left` | A (keycode 65) | deadzone 0.5, auto-repeat |\n| `strafe_right` | D (keycode 68) | deadzone 0.5, auto-repeat |\n\n**Dynamically registered at runtime:** `jump`, `strafe_left`, `strafe_right`, `climb`, `pause`\n\n---\n\n## 5. Gameplay Mechanics\n\n### 5.1 Player Character\n- **Shape:** Capsule (radius 0.3, height 1.0)\n- **Color:** Bright green (`Color(0.2, 0.8, 0.2)`) with subtle emissive glow\n- **Collision Layer:** 8 (LAYER_PLAYER)\n- **Collision Mask:** 7 (Ground=1 + Hazard=4 + Truck=2) — NOTE: Inconsistent with level_manager which sets mask to LAYER_GROUND|LAYER_TRUCK (=3)\n\n### 5.2 Movement Parameters\n| Parameter | Value | Notes |\n|---|---|---|\n| GRAVITY | 25.0 | Higher gravity for snappy platformer feel |\n| JUMP_FORCE | 10.0 | Ground jump |\n| DOUBLE_JUMP_FORCE | 8.5 | Second jump |\n| MAX_JUMPS | 2 | Double jump |\n| MOVE_SPEED | 8.0 | Max lateral speed |\n| AUTO_RUN_SPEED | 5.0 | Forward auto-run velocity (+X) |\n| WALL_JUMP_FORCE_X | 7.0 | Horizontal push on wall jump |\n| WALL_JUMP_FORCE_Y | 10.5 | Vertical on wall jump |\n| WALL_CLIMB_SPEED | 1.5 | Upward climb speed |\n| WALL_SLIDE_SPEED | 2.0 | Max descent while sliding |\n| ACCELERATION | 12.0 | Horizontal acceleration |\n| FRICTION | 0.1 | Horizontal friction |\n| COYOTE_TIME | 0.15s | Grace period after leaving ground |\n| JUMP_BUFFER_TIME | 0.1s | Buffer window for jump input |\n| WALL_COYOTE_TIME | 0.1s | Grace period after leaving wall |\n| WALL_JUMP_INPUT_HANG_TIME | 0.15s | Hold into wall after leaving |\n| MAX_FALL_SPEED | 30.0 | Terminal velocity |\n| WALL_DETECT_LENGTH | 1.0 | Raycast length for wall detection |\n| WALL_DETECT_Y_OFFSET | 0.4 | Y offset for side raycasts |\n\n### 5.3 Player States\n- `is_on_ground` — on floor (from `is_on_floor()` + raycast fallback)\n- `is_on_wall` — touching a wall (from side raycasts)\n- `is_climbing` — manual wall-climb (Q held + wall contact)\n- `jump_count` — 0/1/2 (consumed on each jump)\n- Wall direction: -1 (left wall), +1 (right wall), 0 (no wall)\n\n### 5.4 Truck System\n- **Shape:** Blue box (6 × 3 × 3), metallic emissive glow\n- **Collision Layer:** 2 (LAYER_TRUCK)\n- **Movement:** CharacterBody3D moving along +X at configurable speed\n- **Behavior:**\n  - Speed varies randomly between min_speed and max_speed (target changes every 1.5s)\n  - Z-axis swerving (±1.5 units) changes every 2.5s, clamped to ±3.0 from center\n  - Smooth interpolation (lerp) for speed and swerve changes\n  - Gravity applied if airborne (though trucks should be on ground)\n- **Spacing:** 16 units center-to-center (TRUCK_WIDTH=6.0 + TRUCK_GAP=10.0)\n\n### 5.5 Hazards (4 types, placed on trucks)\n\n| # | Type | Visual | Script | Behavior | Collision |\n|---|---|---|---|---|---|\n| 0 | Saw Blade | Red cylinder with emissive glow (CylinderMesh, r=0.8, h=0.1) | `saw_blade.gd` | Rotates around Z at 12 rad/s; slight tumble on X | SphereShape3D, r=0.8 |\n| 1 | Ramp | Yellow cone (CylinderMesh, r=1.0, h=2.0), emissive | `ramp.gd` | Launches player upward at 18.0 force; 0.5s cooldown | CylinderShape3D, r=1.0, h=2.0 |\n| 2 | Static Debris | Brown sphere (SphereMesh, r=0.5) | None (static) | Stationary marker on truck | SphereShape3D, r=0.5 |\n| 3 | Hammer | Grey box (BoxMesh 0.3×0.3×2), slight glow | `swinging_hammer.gd` | Pendulum swing (amplitude PI/6 to PI/3, period 1.5-3s) | BoxShape3D, 0.3×0.3×2 |\n\n**Falling Debris** — Spawned periodically by LevelManager from above trucks:\n- Brown sphere, SphereShape3D r=0.5, gravity 9.81, max fall time 5.0s\n- Falls independently from truck (not parented)\n\n### 5.6 Hazard Collision Detection\n- Hazards use `body_entered` signal on `Area3D`\n- On collision with player → `GameManager.player_died()`\n- Hazard collision mask: LAYER_PLAYER (=8)\n\n### 5.7 Fall Death\n- If player Y < -10.0 → `player_died.emit()`\n- No lives remaining → game over; otherwise respawn\n\n### 5.8 Finish Condition\n- Player X position ≥ 140.0 (finish_x)\n- Ground is 300 units long (X from -150 to +150)\n- Level completion triggers `GameManager.complete_level()`\n\n---\n\n## 6. Level System & Progression\n\n### 6.1 Five Difficulty Tiers\n\n| Tier | Levels | Trucks (min→max) | Speed (min→max) | Gaps (min→max) | Hazards (min→max) |\n|---|---|---|---|---|---|\n| Tutorial | 1–5 | 1→2 | 10→12 | 3.0→4.0 | 0→1 |\n| Easy | 6–10 | 2→3 | 12→15 | 2.5→3.5 | 1→2 |\n| Medium | 11–20 | 4→6 | 15→18 | 2.0→3.0 | 2→3 |\n| Hard | 21–30 | 6→8 | 18→22 | 1.5→2.5 | 3→4 |\n| Expert | 31–35 | 8→10 | 22→25 | 1.0→2.0 | 4→5 |\n\n### 6.2 Dynamic Level Generation (per level)\n- **Truck count:** Linear interpolation within tier (e.g., medium level 15 = 5 trucks)\n- **Hazard count:** Linear interpolation within tier\n- **Speed:** Lerp between tier min/max based on tier progress\n- **Gap size:** Lerp between tier min/max (decreasing = harder)\n- **Hazard placement:** Each hazard placed on a random truck (cycling if more hazards than trucks)\n- **Hazard types:** Cycle through 0,1,2,3 (saw, ramp, debris, hammer)\n- **Debris falling spawns:** Every 3–5 seconds, debris falls from above a random truck\n\n### 6.3 Save System\n- **Path:** `user://cluster_rush_save.dat` (ConfigFile)\n- **Structure:**\n  - `progress/highest_level` — integer, highest unlocked level\n  - `progress/level_data` — JSON string: `{\"level_num\": {\"stars\": N, \"completed\": bool}}`\n- **Unlock:** Completing level N unlocks level N+1\n- **Stars (1–3):**\n  - ★★★: 3 lives remaining + time bonus ≥ 50\n  - ★★: 2+ lives remaining\n  - ★: Completed (any lives)\n\n### 6.4 Lives\n- Levels 1–5: 3 lives\n- Levels 6–35: 2 lives (configurable in code: `lives = 3 if level_num <= 5 else 2`)\n\n### 6.5 Scoring\n- **Base:** 100 points per level completion\n- **Time bonus:** `min(elapsed_seconds * 10.0, 100.0)` — capped at 100\n- **Total per level:** `100 + time_bonus`\n- Lives lost decrement via `lives -= 1` (no direct score penalty)\n\n---\n\n## 7. UI Elements\n\n### 7.1 HUD (always visible during gameplay)\n- `LevelLabel` — \"Level: N\" (center top, white, 24px)\n- `LivesLabel` — \"Lives: ❤️❤️❤️\" (top-left, emoji hearts)\n- `TimeLabel` — \"Time: MM:SS.ms\" (top-right, white, 24px)\n- `ScoreLabel` — \"Score: N\" (top-right below time, white, 24px)\n- `ProgressBar` — player progress bar (bottom edge, blue fill, 0–100%)\n- Background: Semi-transparent black (`Color(0, 0, 0, 0.6)`) with blue border\n- Updates every 100ms via `_process()`\n- Lives flash yellow on change (0.3s tween back to white)\n\n### 7.2 Overlays (fade-in/out animated transitions)\n- **LevelComplete:** Panel with \"Level Complete!\" + time bonus + \"Next Level\" button\n- **GameOver:** Panel with \"Game Over\" + final score + \"Retry\" / \"Level Select\" / \"Main Menu\" buttons\n- **PauseMenu:** Panel with \"Paused\" + \"Resume\" / \"Level Select\" / \"Main Menu\" buttons\n- **LoadingScreen:** Full-screen overlay with \"Loading...\" + progress bar\n\n### 7.3 Main Menu\n- Dark purple-blue background with rounded corners\n- Four buttons: Start Game, Level Select, Settings, Credits\n- Settings panel: Audio slider (SFX) + Music slider + Close button\n- Toggle visibility of SettingsPanel and PauseMenu within main menu scene\n\n### 7.4 Level Select\n- 7-column GridContainer with 35 dynamically generated buttons\n- Tier color-coding:\n  - Green (`0.2, 0.85, 0.2`) for levels 1–5\n  - Blue (`0.2, 0.5, 0.95`) for levels 6–10\n  - Yellow (`0.95, 0.85, 0.15`) for levels 11–20\n  - Orange (`0.95, 0.55, 0.1`) for levels 21–30\n  - Red (`0.95, 0.15, 0.15`) for levels 31–35\n- Completed levels show star symbols (★, ★★, ★★★)\n- Completed levels have thicker top border\n- Locked buttons are greyed out with grey border\n- Buttons scrollable in ScrollContainer\n- \"Restart\" and \"Back to Menu\" buttons at bottom\n\n### 7.5 End Screen\n- Gold trophy title: \"🏆 All Levels Complete! 🏆\" (36px)\n- Final score display\n- Green \"Back to Main Menu\" button\n\n### 7.6 Credits\n- \"Cluster Rush\" (48px)\n- RichTextLabel with game info (\"A truck-climbing platformer.\", \"Engine: Godot 4.7\", \"Built with love.\")\n\n---\n\n## 8. Audio System\n\n### 8.1 SFX Files\n| File | Purpose | Trigger |\n|---|---|---|\n| `audio/sfx/jump.wav` | Jump sound | Ground jump |\n| `audio/sfx/land.wav` | Landing sound | First contact after jump |\n| `audio/sfx/death.wav` | Death sound | Player death |\n| `audio/sfx/hit.wav` | Hazard contact | Player hits hazard |\n| `audio/sfx/wall_slide.wav` | Wall slide SFX | While sliding a wall |\n| `audio/sfx/wall_jump.wav` | Wall jump sound | Wall jump |\n| `audio/music/bgm_around.wav` | Background music | (Referenced but no music player exists) |\n\n### 8.2 Audio Implementation\n- `AudioManager` autoload: Only stores volume values (master, SFX). The `play_sfx()` method is a **stub/placeholder** — it only prints and does not actually play audio.\n- `player_movement.gd` has an `AudioStreamPlayer` child node (`SFX`) for playing SFX directly.\n- Music is loaded (`bgm_around.wav`) but no `AudioStreamPlayer` is set up for music playback.\n- Settings UI has an SFX volume slider and a Music volume slider.\n\n---\n\n## 9. Particle Effects System\n\nClass: `ParticleEffects` (`scripts/utilities/particle_effects.gd`)\n- Uses procedural quad meshes (no texture file dependency)\n- Custom `ParticleProcessMaterial` with Gradient color ramp and Curve2D scale ramp\n- Types:\n\n| Type | Color | Count | Lifetime | Spread |\n|---|---|---|---|---|\n| Jump | Green (0.2, 0.9, 0.3) | 12 | 2.0s | 0.5 |\n| Land | Yellow (0.8, 0.8, 0.4) | 8 | 1.5s | 0.3 |\n| Death | Red (1.0, 0.2, 0.1) | 25 | 4.0s | 1.0 |\n| Complete | Gold (1.0, 1.0, 0.2) | 30 | 3.0s | 1.5 |\n| Dust | Grey (0.6, 0.6, 0.5) | 6 | 1.0s | 0.2 |\n| Sparks | Yellow-white (1.0, 0.9, 0.5) | 15 | 3.0s | 0.5 |\n\n- Each particle: `one_shot=true`, `autofree=true` (auto-removes after animation)\n- Acceleration: -100 units/s² on Y (particles quickly fall)\n- Randomness: 0.8 (high dispersion)\n\n---\n\n## 10. Collision Layers & Masks\n\n| Layer | Constant | Value | Used By |\n|---|---|---|---|\n| Ground | LAYER_GROUND | 1 | Static ground platform |\n| Truck | LAYER_TRUCK | 2 | Moving trucks |\n| Hazard | LAYER_HAZARD | 4 | All hazard Area3Ds |\n| Player | LAYER_PLAYER | 8 | Player CharacterBody3D |\n\n| Mask | Expression | Detects |\n|---|---|---|\n| MASK_PLAYER | LAYER_GROUND \\| LAYER_TRUCK | Player detects ground and trucks |\n| MASK_HAZARD | LAYER_PLAYER | Hazards detect player body |\n| MASK_TRUCK | LAYER_GROUND \\| LAYER_PLAYER | Trucks detect ground and player |\n\n**Player collision_mask in player_movement.gd:** 7 (Ground|Hazard|Truck)\n**Player collision_mask in level_manager.gd:** 3 (Ground|Truck) — **INCONSISTENCY**\n\n---\n\n## 11. Camera Systems\n\n### 11.1 FollowCamera (`scripts/camera/camera_controller.gd`)\n- Class name: `FollowCamera`\n- Third-person camera behind player\n- Parameters:\n  - `follow_distance`: 8.0 units behind player\n  - `height_offset`: 4.0 units above player\n  - `smoothness`: 0.12 (lerp factor)\n  - `rotation_speed`: 2.0 rad/s\n  - `vertical_smoothness`: 0.1\n  - `look_ahead`: 3.0 units (based on player velocity)\n- Smoothly follows player position with velocity-based lookahead\n\n### 11.2 FirstPersonCamera (`scripts/camera/first_person_camera.gd`)\n- First-person POV camera\n- Follows player at position + (0, 1.5, 0)\n- Matches player rotation.y\n- Subtle vertical sway when airborne: `sin(time * 3.0) * 0.02`\n- Scene file uses this script (not FollowCamera)\n\n---\n\n## 12. Rendering & Visuals\n\n- **Renderer:** Forward Plus (`renderer_backend=0`)\n- **MSAA 3D:** 2x\n- **Two directional lights:**\n  - Main: 45°X, 30°Y, shadows ON\n  - Fill: -30°X, -20°Y\n- **No post-processing, shadows on fill light are disabled**\n- **No custom shaders** found in the codebase\n- **Materials:** All use `StandardMaterial3D` with albedo color, roughness, metallic, and emissive glow\n- **Visual materials per object type:**\n  - Ground: Grey (0.3, 0.3, 0.3), roughness 0.9, metallic 0.0\n  - Truck: Blue (0.2, 0.6, 0.9), roughness 0.3, metallic 0.7, emissive glow\n  - Player: Green (0.2, 0.8, 0.2), roughness 0.5, metallic 0.2, emissive glow\n  - Saw: Red (0.9, 0.2, 0.2), roughness 0.2, metallic 0.9, emissive (1.0, 0.1, 0.1)\n  - Ramp: Yellow-orange (0.9, 0.7, 0.2), roughness 0.5, metallic 0.3, emissive\n  - Hammer: Grey (0.4, 0.4, 0.4), roughness 0.4, metallic 0.8, emissive\n  - Debris: Brown (0.5, 0.3, 0.1), roughness 0.95, metallic 0.0\n\n---\n\n## 13. Technical Details\n\n### 13.1 Physics\n- **Gravity:** 25.0 units/s² (custom, higher than Godot default 9.81)\n- **Physics integration:** `CharacterBody3D.move_and_slide()` (built-in physics)
- **RayCast3D:** Three raycasts on player (left, right, ground); collide with bodies AND areas
- **Raycast lengths:** Wall = 1.0, Ground = 0.5
- **Player capsule:** radius 0.3, height 1.0

### 13.2 Scene Loading
- Level generation via `LevelManager.load_level()` → `_generate_level()`
- Clears dynamic nodes from World node, preserves scene-prefab nodes (Player, Camera3D, lights)
- Rebuilds ground, trucks, hazards, and player position each level transition
- Scene reload via `get_tree().reload_current_scene()` for retry/next-level

### 13.3 Transitions
- Fade-in/out animations via `Tween` (0.4s in, 0.3s out)
- `modulate` alpha channel used for overlay transparency
- `_is_transitioning` flag prevents input during transitions

### 13.4 WebGL Bridge (Commented Out)\n- `webgl_bridge.gd` is **commented out** in `project.godot` autoloads
- Provides 30+ `bridge_*` methods exposed to JavaScript
- Tracks player position, state, FPS, heap size
- Provides test methods: `bridge_trigger_jump()`, `bridge_trigger_fall_scenario()`, `bridge_move_player_left()`, etc.\n- Used for Playwright E2E testing (see `tests/e2e/`)

### 13.5 GUT Testing Framework\n- `addons/gut/` — GUT unit testing framework included\n- Test files present:\n  - `tests/unit/test_level_templates.gd`\n  - `tests/unit/test_smoke.gd`\n  - `tests/integration/test_gameplay_core.gd`\n  - `tests/integration/test_levels.gd`\n  - `tests/integration/test_scene_instantiation.gd`\n  - `tests/integration/test_save_migration.gd`\n\n### 13.6 Playwright E2E Tests\n- `tests/e2e/` — Playwright TypeScript tests\n- `tests/e2e/cluster_rush.spec.ts` — Main E2E test file\n- `tests/e2e/level_select.spec.ts` — Level select tests\n- `tests/e2e/m5_campaign_e2e.spec.ts` — Campaign/progression tests\n- `tests/e2e/perf.spec.ts` — Performance tests\n- Uses canvas-based helper: `tests/e2e/helpers/canvas.ts`\n- Probes for level 1, M5 level, trajectory testing\n\n### 13.7 Build Output\n- `Builds/WebGL/index.html` — Godot WebGL build\n- JavaScript WASM binary: `index.wasm`\n- Packed data: `index.pck`\n- Audio worklets: `index.audio.worklet.js`, `index.audio.position.worklet.js`\n- Single-threaded export (no `SharedArrayBuffer`)\n- Virtual port 1280×720\n\n---\n\n## 14. Assets Inventory\n\n### 14.1 Audio\n- `audio/music/bgm_around.wav` — Background music\n- `audio/sfx/death.wav` — Death sound\n- `audio/sfx/hit.wav` — Hazard contact\n- `audio/sfx/jump.wav` — Jump sound\n- `audio/sfx/land.wav` — Landing sound\n- `audio/sfx/wall_jump.wav` — Wall jump\n- `audio/sfx/wall_slide.wav` — Wall slide\n\n### 14.2 Icons / Images\n- `icon.svg` — Project icon (SVG)\n- Various `.import` files for asset caching (Godot format)\n\n### 14.3 No External Image Assets\n- All 3D visuals use procedural meshes (BoxMesh, CylinderMesh, SphereMesh, CapsuleMesh)\n- All materials use procedural colors (no image textures loaded)\n- Particles use procedural quad meshes (no image files)\n\n### 14.4 Fonts\n- Uses Godot system font (`ThemeDB.fallback_font`) — no custom font files loaded\n- Various font files exist in GUT addon (`fonts/`) but not used by the game itself

### 14.5 Additional Projects (in same repo)
- `geometry-dash/` — Separate geometry dash clone (vanilla JS/vite)
- `tic-tac-toe/` — Simple tic-tac-toe game
- `2048/2048.html` — 2048 game in HTML
- `www/` — Web server files (cors_server.py, server.py, service-worker.js, manifest.json, .htaccess)

---\n\n## 15. Known Limitations & Fragile Areas\n\n### 15.1 Audio System Is Broken\n- **Critical:** `AudioManager.play_sfx()` is a **stub** — it does nothing (only prints). The actual SFX player (`SFX` node on player) is wired in `player_movement.gd._ready()`, but `AudioManager` has no actual audio playback infrastructure.\n- Music (`bgm_around.wav`) is referenced but no `AudioStreamPlayer` exists for music playback.\n- The settings UI attempts to call `AudioManager.get_settings()` which does not exist.\n\n### 15.2 Collision Mask Inconsistency\n- `player_movement.gd._ready()`: `collision_mask = 7` (Ground|Hazard|Truck)\n- `level_manager.gd._place_player()`: `collision_mask = MASK_PLAYER = 3` (Ground|Truck)\n- If LevelManager creates the player, the mask is 3 (player won't detect hazards, so hazards can't trigger death)\n\n### 15.3 Hazard Collision Uses body_entered on Area3D\n- Player is a `CharacterBody3D`, not a node that fires `body_entered` on areas\n- The `Area3D.body_entered` signal fires when another Area3D or PhysicsBody3D enters\n- This may work with `CharacterBody3D` in Godot 4.x, but it's fragile and may not detect all collisions reliably\n- Better approach: use Area3D `area_entered` with player's collision area, or use the player's `body_entered` signal\n\n### 15.4 Saw Blade Rotation Anomaly\n- Saw blade visual rotates on both Z (`rotation.z += rotation_speed * delta`) and X (`rotation.x += rotation_speed * delta * 0.1`)\n- The X rotation causes a subtle tumble — may look odd or be unintended\n\n### 15.5 Input Action Redundancy\n- `ui_left` and `strafe_left` both map to A key\n- `ui_right` and `strafe_right` both map to D key\n- The game uses `get_axis(\"strafe_left\", \"strafe_right\")` — but the input map also has `ui_left`/`ui_right` which are not used by game code\n\n### 15.6 Pause Input Conflict\n- Both `InputManager._unhandled_input()` and `main_menu_ui.gd._process()` handle `pause` action\n- Main menu scene is only active in menu state, but both handlers could fire if input propagates unexpectedly\n- `project.godot` defines `pause` with keycode 4194310 (unknown mapping), and no keyboard key is assigned\n\n### 15.7 WebGLBridge Is Disabled\n- `webgl_bridge.gd` is commented out in `project.godot` autoloads\n- All 30+ JS bridge methods are unreachable\n- E2E tests may fail because of this (they likely depend on bridge methods like `bridge_get_player_y()`)\n\n### 15.8 Player Detection in Multiple Scripts\n- Multiple scripts attempt to find the player node independently:\n  - `InputManager`: `get_node_or_null(\"/root/World/Player\")`\n  - `WebGLBridge`: `has_node(\"World/Player\")`\n  - `HUD`: finds player via `get_tree().get_current_scene().World.Player`\n  - `FirstPersonCamera`: 4 different lookup strategies\n  - `FollowCamera`: 5 different lookup strategies\n- All fragile — if node hierarchy changes, all break silently\n\n### 15.9 Level Generation Relies on Scene Prefs\n- `_generate_level()` skips removing nodes named \"Player\", \"Camera3D\", \"DirectionalLight3D\", \"DirectionalLight3D2\"\n- If these nodes don't exist in the scene file, they're created dynamically\n- But dynamic creation of player doesn't add raycasts (RayCastLeft, RayCastRight, GroundCheck)\n\n### 15.10 No Touch/Controller Input Support\n- Input map only defines keyboard actions (A, D, W, S, Space, Q, R)\n- No touch input, no mouse input, no gamepad input defined\n- `touch-action: none` in WebGL HTML suggests awareness but no implementation\n\n### 15.11 Potential Division by Zero\n- `level_select_ui.gd._apply_button_style()` calls `LevelManager.get_tier_color(btn.text.strip_edges().replace(\"L\", \"\").to_int())` on a button that may have stars appended (\"L1 ★★★\") — the `to_int()` after `replace(\"L\", \"\")` could parse incorrectly if stars are present\n\n### 15.12 Camera Script Naming\n- `first_person_camera.gd` is a Camera3D but named as if it's first-person, while `camera_controller.gd` (FollowCamera) is a Node3D wrapper. The game scene uses `first_person_camera.gd` directly on Camera3D, making it effectively third-person despite the name.\n\n### 15.13 Level Completion Timer Double-Count\n- `GameManager.complete_level()` calculates time bonus using `Time.get_ticks_msec()`\n- But the `_on_level_completed()` handler in `game_scene.gd` recalculates the same bonus and displays it separately\n- The two values may differ slightly due to frame timing\n\n### 15.14 Debris Timer Always Active\n- `_spawn_falling_debris()` runs in `_process()` as long as `_debris_target_speed > 0`, which is set to 10.0 and never reset\n- Debris spawns persist even after level completion until scene reload\n- If debris accumulation becomes heavy, could cause performance issues in long sessions\n\n### 15.15 No Sound on Death/Complete\n- `game_scene.gd` calls `player.die()` on death, which sets `_death_sound` and plays it\n- But `complete_level()` doesn't play any SFX — no sound for level completion or hazard contact\n\n### 15.16 Scene Transition Race Condition\n- `_on_next_level()` calls `get_tree().reload_current_scene()` but doesn't await scene ready before returning\n- If `GameManager.current_level` is updated before the new scene's `LevelManager.load_level()` fires, it could load the wrong level\n\n### 15.17 No Sound File Existence Check on Music\n- `player_movement.gd._ready()` checks `FileAccess.file_exists()` for all SFX files\n- No equivalent check exists for `bgm_around.wav` in any music playback code (because no music playback code exists)\n\n### 15.18 Hazard Cooldown Per-Type, Not Per-Hazard\n- `ramp.gd` has a 0.5s `_launch_cooldown` per instance\n- But saw blades and hammers have no cooldown — they can damage the player every frame while in contact\n- The `on_ground_changed` signal is never connected to a listener that would use it\n\n### 15.19 No Multiplayer\n- Single-player only, no network code, no multiplayer abstraction\n\n### 15.20 Auto-Run Direction Assumption\n- `AUTO_RUN_SPEED = 5.0` is applied to `velocity.x` — but movement input is on `velocity.z`\n- The X axis is the forward direction, Z is lateral (left/right), Y is up — this is an unconventional axis mapping that may cause confusion\n\n### 15.21 Unused Variables & Dead Code\n- `InputManager._input_buffer` is declared but never used (only `_input_queue` is used)\n- `InputManager._last_ground_contact` is set in `_process()` but `can_jump()` checks `time_since_ground <= COYOTE_TIME` — there's a timing issue: `_process()` runs on frame rate, while jump check may fire in the same or earlier frame\n- `particle_effects.gd._particle_material` is created in `_ready()` but never actually used (each particle creation creates its own `ParticleProcessMaterial`)\n- `main_menu_ui.gd._settings_open` is never set\n- `swing_hammer.gd` has `knockback_force` export but it's never used\n- `ramp.gd` has `ramp_length` and `ramp_height` exports but they're only used in a commented-out shape setup\n\n"}